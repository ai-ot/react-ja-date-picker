---
machine:
  timezone: "Asia/Tokyo"
  node:
    version: 6.9.3
  post:
    - curl -o- -L https://yarnpkg.com/install.sh | bash

dependencies:
  cache_directories:
    - "~/.cache/yarn"

test:
  pre:
    - "npm run build"
  override:
    - "npm test"

notify:
  webhooks:
    - url: "https://hooks.slack.com/services/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"
